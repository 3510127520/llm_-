# 📁 文件类型统计工具

一个用 Python 写的文件扫描统计工具，能扫描文件夹、统计文件类型，还支持按扩展名过滤和按文件名搜索。

## 功能

- 扫描指定文件夹（包括子文件夹）
- 统计各类型文件数量
- 支持按扩展名过滤（如只统计 `.txt`）
- 支持按文件名关键词搜索（如只显示包含 `report` 的文件）
- 生成报告并保存为 `report.txt`
- 友好的错误提示（路径不存在也不会崩溃）

## 使用方法

```bash
python 4tian.py