Embedding 是把文本转换成向量数字的过程。

Token 被输入模型后，模型会输出一个向量（一串数字），代表该 Token 的语义信息。
相似语义的 Token 在向量空间中距离更近。

常用的 Embedding 模型：
- OpenAI text-embedding-3-small（1536 维）
- BGE 系列（中文优化）
- Sentence-Transformers（本地部署）

在 RAG 中，Embedding 用于计算用户问题和知识库文档的语义相似度。